adding std.io
adding interference.sound

call main() -> int {
    string loud = interference.sound.constructive_sound_interference(1.0, 0.7, 0.0)
    string quiet = interference.sound.destructive_sound_interference(1.0, 0.7, 3.14159)
    string beats = interference.sound.sound_beats(440.0, 444.0, 1.0, 1.0)
    inscribe("loud:", loud.amplification_factor)
    inscribe("quiet:", quiet.attenuation_factor)
    inscribe("beats:", beats.beat_frequency)
    return 0
}
