adding std.io

using mermaid.struct;
using mermaid.cluster;

adding <j> j_len(text: string) -> int {
    return text.length();
}

call main() -> int {
    int n = j_len("mermaid java")
    inscribe("java len:", n)
    return 0
}
