adding std.io
adding quantum.core

call main() -> int {
    quant_i q0 = |0>
    quant_i q1 = |0>
    quant_i[] reg = [q0, q1]
    reg = CNOT(reg, 0, 1)
    inscribe("size:", |reg|)
    inscribe("m0:", measure(reg[0]))
    inscribe("m1:", measure(reg[1]))
    return 0
}
