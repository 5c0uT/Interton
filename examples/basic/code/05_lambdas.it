adding std.io

call apply_twice(x: int, f: (int) -> int) -> int {
    return f(f(x))
}

call main() -> int {
    int v = apply_twice(3, (n) -> n + 2)
    int w = apply_twice(5, (n) -> n * n)
    inscribe("twice +2:", v)
    inscribe("twice square:", w)
    return 0
}
