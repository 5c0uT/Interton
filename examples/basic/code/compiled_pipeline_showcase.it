adding std.io

call main() -> int {
    int sum = 0
    for int i = 0; i < 5; i = i + 1 {
        sum = sum + i
    }

    if sum > 5 {
        inscribe("sum:", sum)
    } else {
        inscribe("small:", sum)
    }

    return 0
}
