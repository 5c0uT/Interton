adding std.io
adding interf.core
adding interf.math

call main() -> int {
    interf_f series = 0.0
    for int i = 0; i < 5; i = i + 1 {
        series = float(i) * 1.5
    }
    inscribe("len:", |series|)
    inscribe("mean:", interf.math.mean(series))
    return 0
}
